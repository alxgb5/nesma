{
  "name": "@alxgb/nesma-client",
  "version": "1.0.1",
  "description": "API Client code generated from nesma project",
  "main": "./swagger/index.ts",
  "types": "./swagger/index.d.ts",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "version": "npm run format && git add -A core",
    "postversion": "git push && git push --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api"
  ],
  "author": "Alexandre Brun-Giglio",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "url-parse": "^1.5.10"
  }
}
